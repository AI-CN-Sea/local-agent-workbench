# Coding Task Example

## example user input

Build a small Python command-line tool that reads a CSV file path from arguments, validates the headers, and prints a local summary. Include a minimal test plan.

## expected skill

`skill-coding`

## expected output type

Implementation plan, code-oriented steps, and review summary.

## privacy level

P1 local-only. No external API, no web search, no uploaded file parsing unless explicitly approved in a future sandboxed workflow.